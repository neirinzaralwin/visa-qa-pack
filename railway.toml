[build]
builder = "nixpacks"

[deploy]
startCommand = "cd api && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
